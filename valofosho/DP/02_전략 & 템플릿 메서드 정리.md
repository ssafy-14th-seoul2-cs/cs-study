---

# 🧩 전략 패턴 & 템플릿 메서드 패턴 정리

---

# 1️⃣ 전략 패턴 (Strategy Pattern)

## 📌 개념

> **행위를 객체로 캡슐화하여**, 런타임에 알고리즘(전략)을 자유롭게 교체할 수 있도록 하는 패턴.

즉,

* “정렬 방식을 바꾼다”,
* “결제 방식을 바꾼다”,
* “로그 출력 방식을 바꾼다”
  같이 **행위를 쉽게 변경해야 하는 경우** 사용.

---

## 🧠 핵심 아이디어

| 구성 요소                 | 역할                      |
| --------------------- | ----------------------- |
| **Strategy 인터페이스**    | 알고리즘 기준이 되는 공통 메서드      |
| **Concrete Strategy** | 실제 알고리즘 구현              |
| **Context**           | 전략을 사용하는 객체 → 전략을 교체 가능 |

---

## 🧩 구조 그림

```
[Context] ----> [Strategy 인터페이스]
                   ↑           ↑
          [ConcreteA]   [ConcreteB]
```

---

## 🐍 Python 예제 — 결제 전략 변경하기

```python
from abc import ABC, abstractmethod

# 전략 인터페이스
class PaymentStrategy(ABC):
    @abstractmethod
    def pay(self, amount):
        pass

# 구체 전략들
class CardPayment(PaymentStrategy):
    def pay(self, amount):
        print(f"Paying {amount} using Card")

class PaypalPayment(PaymentStrategy):
    def pay(self, amount):
        print(f"Paying {amount} using PayPal")

# Context
class PaymentProcessor:
    def __init__(self, strategy: PaymentStrategy):
        self.strategy = strategy

    def set_strategy(self, strategy: PaymentStrategy):
        self.strategy = strategy

    def process(self, amount):
        self.strategy.pay(amount)


# 사용
processor = PaymentProcessor(CardPayment())
processor.process(100)     # Paying 100 using Card

processor.set_strategy(PaypalPayment())
processor.process(200)     # Paying 200 using PayPal
```

---

## 👍 장점

* 행위(알고리즘)를 객체 단위로 분리 → **확장성 ↑**
* if/elif/switch 로직 제거
* 런타임에 전략 교체 가능
* 단일 책임 분리

## 👎 단점

* 전략 클래스가 많아짐
* Context + Strategy 조합 이해 필요

---

# 2️⃣ 템플릿 메서드 패턴 (Template Method Pattern)

## 📌 개념

> **상위 클래스에서 알고리즘(전체 흐름) 틀을 정의하고**,
> **일부 단계는 서브클래스가 오버라이드해서 변경 가능하도록 하는 패턴.**

즉,

* 전체 프로세스는 같지만
* 세부 단계만 상황에 따라 바뀌는 경우 사용.

---

## 🧠 핵심 아이디어

| 역할                 | 설명                             |
| ------------------ | ------------------------------ |
| **Abstract Class** | 전체 알고리즘 골격(Template Method) 제공 |
| **Concrete Class** | 필요한 부분(훅 메서드)만 재정의             |

---

## 🧩 구조 그림

```
[AbstractClass]
    ├─ template_method() ← 변하지 않는 큰 흐름
    ├─ step1() ← 기본 or 추상
    └─ step2()

        ↑
[ConcreteClassA]
[ConcreteClassB]
```

---

## 🐍 Python 예제 — 데이터 처리 파이프라인

```python
from abc import ABC, abstractmethod

class DataPipeline(ABC):
    def process(self):
        self.load_data()
        self.transform()
        self.save()

    @abstractmethod
    def load_data(self):
        pass

    @abstractmethod
    def transform(self):
        pass

    def save(self):  # 공통 동작
        print("Saving processed data")

class CSVDataPipeline(DataPipeline):
    def load_data(self):
        print("Loading CSV data")

    def transform(self):
        print("Transforming CSV data")

class JSONDataPipeline(DataPipeline):
    def load_data(self):
        print("Loading JSON data")

    def transform(self):
        print("Transforming JSON data")


# 사용
pipeline = CSVDataPipeline()
pipeline.process()
# Loading CSV data
# Transforming CSV data
# Saving processed data
```

---

## 👍 장점

* 알고리즘(전체 흐름)을 재사용 가능
* 상속 기반으로 단계별 유연한 변경 가능
* 중복 코드 감소

## 👎 단점

* **상속 기반 → 유연성이 떨어질 수 있음**
* 단계가 많아지면 복잡해짐

---

# 3️⃣ 전략 vs 템플릿 — 둘의 차이

| 항목         | 전략 패턴               | 템플릿 메서드                          |
| ---------- | ------------------- | -------------------------------- |
| **핵심 목적**  | 알고리즘(전략)을 **바꿔끼기**  | 알고리즘 **전체 틀은 고정**, 일부만 오버라이드     |
| **방식**     | **위임(Composition)** | **상속(Inheritance)**              |
| **확장성**    | 자유롭게 전략 추가 가능       | 상속 구조 깊어지면 복잡                    |
| **언제 사용?** | “행위를 교체해야 할 때”      | “고정된 알고리즘 흐름이 있을 때”              |
| **대표 사용처** | 결제 방식, 정렬 전략        | 데이터 처리 파이프라인, Template Rendering |

---
