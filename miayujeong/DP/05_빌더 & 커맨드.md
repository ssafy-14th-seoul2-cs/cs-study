## 📖 빌더 패턴 (Builder Pattern)

- 복잡한 객체 생성 과정을 단계적으로 분리하고 가독성을 높이는 패턴
- 생성자(constructor)가 너무 복잡하거나 파라미터가 많을 때 사용
- 객체의 구성 과정(steps) 을 분리하고, “어떻게” 만들지는 빌더가 결정함
- 클라이언트는 구성 순서를 지시(Director) 만 함
- 객체 인스턴스 생성과 동시에 값을 주입함

<br>

### 빌더 패턴을 사용하는 경우

- 생성자 파라미터가 너무 많을 때 (`new Car("red", 2000cc, true, ... )`)
- 설정 옵션이 다양해지는 객체가 있을 때
- 객체 생성 과정을 유연하게 제어하고 싶을 때
- 동일한 생성 과정에서 다른 형태의 객체를 만들고 싶을 때

<br>

### 작동 방식

#### 구조

```python
Director → Builder → ConcreteBuilder → Product
```

#### Python 예시

```python
class Car:
    def __init__(self, color=None, engine=None, sunroof=False):
        self.color = color
        self.engine = engine
        self.sunroof = sunroof

    def __str__(self):
        return f"Car(color={self.color}, engine={self.engine}, sunroof={self.sunroof})"

class CarBuilder:
    def __init__(self):
        self.car = Car()

    def set_color(self, color):
        self.car.color = color
        return self

    def set_engine(self, engine):
        self.car.engine = engine
        return self

    def add_sunroof(self):
        self.car.sunroof = True
        return self

    def build(self):
        return self.car

# 사용
car = (
    CarBuilder()
        .set_color("blue")
        .set_engine("2000cc")
        .add_sunroof()
        .build()
)

print(car)
```

<br>

### 장단점

| 장점 | 단점 |
| --- | --- |
| 복잡한 객체 생성 코드 단순화 | Director·Builder 구조가 다소 복잡해짐 |
| 객체 생성 과정을 단계별로 제어 가능 | 작은 객체에는 오버엔지니어링 |
| 다양한 조합의 객체 생성 가능 (유연성) | Builder 클래스를 여러 개 생성해야 할 수도 있음 |
| 가독성↑,  유지보수성↑ | 초기 설계 시간 증가 |

<br>

## 📖 커맨드 패턴 (Command Pattern)

- 실행될 기능을 객체(Command) 로 캡슐화하는 패턴
- 요청(행동)을 객체로 만들기 때문에, 실행/취소/저장/재실행 등을 쉽게 구현할 수 있음
- 버튼 클릭, 메뉴 실행, 작업 기록(undo/redo), 매크로 기능 등에 자주 사용됨
- 객체 간의 결합도를 낮추고 유연성을 높이는 패턴

<br>

### 커맨드 패턴을 사용하는 경우

- “요청을 큐에 저장했다가 나중에 실행”해야 하는 경우
- UI 버튼 → 특정 동작 매핑
- Undo/Redo 기능
- 매크로(여러 명령 조합하여 한 번에 실행)
- 네트워크/비동기 작업 처리

<br>

### 작동 방식

#### 구조

```python
Client → Invoker → Command → Receiver
```

#### Python 예시

```python
class Light:
    def on(self):
        print("💡 불 켜짐")
    def off(self):
        print("💡 불 꺼짐")

class OnCommand:
    def __init__(self, light):
        self.light = light

    def execute(self):
        self.light.on()

class OffCommand:
    def __init__(self, light):
        self.light = light

    def execute(self):
        self.light.off()

class RemoteControl:
    def __init__(self):
        self.commands = []

    def submit(self, cmd):
        self.commands.append(cmd)
        cmd.execute()

light = Light()
remote = RemoteControl()

remote.submit(OnCommand(light))
remote.submit(OffCommand(light))
```

<br>


### 장단점

| 장점 | 단점 |
| --- | --- |
| 실행 로직과 요청을 완전히 분리 | 파일 수 증가(클래스 남발) 가능 |
| Undo/Redo, 기록 관리 용이 | 단순한 시스템엔 오히려 복잡해짐 |
| 매크로, 큐 기반 작업 처리 가능 | 구조 설계가 어려울 수 있음 |
| OCP 만족 → 새로운 명령 추가 쉬움 |  |

<br>

## 💭 Builder vs. Command

| 항목 | Builder | Command |
| --- | --- | --- |
| 목적 | **복잡한 객체 생성 분리** | **요청/행동의 캡슐화** |
| 초점 | 객체 구성(steps) | 실행 동작(undo, redo, queue) |
| 주요 구분 | Director / Builder / Product | Invoker / Command / Receiver |
| 사용 시점 | 생성자 파라미터 많을 때 | 버튼/메뉴/이벤트 기반 시스템 |
| 예시 | 자동차, 메시지, 설정 객체 | 불 켜기, 파일 저장, 작업 큐 |